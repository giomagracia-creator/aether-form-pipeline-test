# Contact form is demo-only (UI mock — not wired to email).
# No Formspree / Resend env vars required right now.
#
# Later, when wiring real email, you may add something like:
#   NEXT_PUBLIC_FORMSPREE_FORM_ID=your_form_id
# or server-side Resend keys (never commit real secrets).
